# CLAUDE.md — ClawShell 代码规范与行为准则

## 项目定位

ClawShell 是一个适用于类 OpenClaw 架构的增强型外骨骼功能插件，具备自感知、自适应、自组织、多Agent集群四层能力。以工程控制论为指导思想，遵循无侵入、低耦合、高鲁棒、高泛用、高协同、高扩展的设计原则。

## 行为准则

### 1. 先思考，再编码

- 实现前先陈述假设，不确定时主动询问
- 存在多种解读时，呈现选项而非自行选择
- 有更简单的方案时直接指出
- 遇到不明白的地方停下来，明确说出困惑点

### 2. 简洁优先

- 只写解决问题所需的最少代码
- 不添加未被要求的功能
- 不为单次使用创建抽象
- 不添加未被请求的"灵活性"或"可配置性"
- 不对不可能发生的场景做错误处理
- 如果 200 行能写成 50 行，重写它
- 自问："资深工程师会觉得这过度复杂吗？"，如果是，简化

### 3. 精准修改

- 只改必须改的，只清理自己造成的混乱
- 不"改进"相邻代码、注释或格式
- 不重构没有坏的东西
- 匹配现有代码风格，即使你有不同偏好
- 发现无关的死代码时告知，但不擅自删除
- 删除因你的修改而成为孤立的 import/变量/函数
- 不被要求时不删除已存在的死代码
- 测试标准：每个被修改的行都应能追溯到用户的需求

### 4. 目标驱动执行

- 将任务转化为可验证的目标
- "添加验证" → "先写测试，再让测试通过"
- "修复bug" → "先写复现测试，再修复"
- "重构X" → "确保测试前后都通过"
- 多步骤任务先陈述简要计划，每步带验证标准
- 强的成功标准让你独立迭代；弱的标准需要不断确认

## 技术约定

- Python 代码遵循项目现有风格（stdlib logging、threading 模型）
- cloud engines 保持 stdlib-only 原则（不引入 asyncio）
- 模块间通过 EventBus + 文件协议通信，不直接调用
- 新模块必须满足：可感知（注册+健康检查）、可适应（降级+参数调整）、可协作（EventBus+TaskMarket）

## 目录结构

```
ClawShell/
├── cloud/           # Cloud Hub 云枢 — FastAPI + 引擎 + Event Sourcing
├── edge/            # Edge Brain 端脑 — 检测/桥接/同步/适配
├── exoskeleton/     # 外骨骼核心 — L1~L4 + Genome
├── shared/          # 共享层 — 类型/模型/工具
├── tests/           # 测试
├── docs/            # 文档
├── scripts/         # 运维脚本
└── deploy/          # 部署配置 (Terraform/Docker)
```
